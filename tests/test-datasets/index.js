/**
 * Test Datasets Index
 * 
 * Exports all test datasets for use in UltraLink tests
 */

const { createActiveInferenceLabDataset } = require('./active-inference-lab');

module.exports = {
  createActiveInferenceLabDataset
}; 